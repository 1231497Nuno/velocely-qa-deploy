# ============================================================
# Velocely — ambiente DEV
# ============================================================
# Copia para .env.dev (NÃO commits). Mesmo cluster Atlas; BD separada.
# Arranque: git checkout DEV → iniciar/Mac/INICIAR.command (ou Windows)
# ============================================================

MONGO_URL="mongodb+srv://USER:PASSWORD@cluster0.XXXXX.mongodb.net/?retryWrites=true&w=majority&appName=Velocely"
DB_NAME=velocely
COMPOSE_PROJECT_NAME=velocely-dev

JWT_SECRET=SUBSTITUI-ISTO-pelo-openssl-rand-hex-32
ADMIN_EMAIL=admin@velocely.local
ADMIN_PASSWORD=MudaIsto123!

CORS_ORIGINS="http://localhost:3080,http://127.0.0.1:3080,http://localhost:3000,http://127.0.0.1:3000"
REACT_APP_BACKEND_URL=
WEB_PUBLISH_PORT=3080
API_PUBLISH_PORT=8000
