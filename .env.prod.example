# ============================================================
# Velocely — ambiente PROD (futuro)
# ============================================================
# Copia para .env.prod quando fores a produção. BD dedicada no Atlas.
# ============================================================

MONGO_URL="mongodb+srv://USER:PASSWORD@cluster0.XXXXX.mongodb.net/?retryWrites=true&w=majority&appName=Velocely"
DB_NAME=velocely_prod
COMPOSE_PROJECT_NAME=velocely-prod

JWT_SECRET=SUBSTITUI-ISTO-pelo-openssl-rand-hex-32
ADMIN_EMAIL=admin@velocely.local
ADMIN_PASSWORD=MudaIstoParaProducao!

CORS_ORIGINS="https://teu-dominio.com"
REACT_APP_BACKEND_URL=
WEB_PUBLISH_PORT=3080
API_PUBLISH_PORT=8000
